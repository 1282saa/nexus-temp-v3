# Backend Environment Configuration Template
# Copy this file to .env and fill in your actual values
# DO NOT commit .env file to git

# Deployment Stage
STAGE=dev

# AWS Region
REGION=us-east-1

# Service Name
SERVICE_NAME=nexus-backend

# AWS Cognito Configuration - 자신의 Cognito User Pool 정보 입력
COGNITO_USER_POOL_ID=us-east-1_XXXXXXXXX
COGNITO_CLIENT_ID=YOUR_COGNITO_CLIENT_ID

# Optional: Custom Domain (if you have one)
CUSTOM_DOMAIN=

# Optional: AWS Profile (if using named profiles)
AWS_PROFILE=

# DynamoDB Table Prefix (optional, defaults to service name)
TABLE_PREFIX=nexus

# Lambda Memory Size (optional, defaults to 512)
LAMBDA_MEMORY=512

# Lambda Timeout (optional, defaults to 60)
LAMBDA_TIMEOUT=60