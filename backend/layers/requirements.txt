# Lambda Layer 의존성
# 모든 Lambda 함수에서 공통으로 사용하는 라이브러리

# AWS SDK (최신 버전)
boto3==1.34.14
botocore==1.34.14

# 필수 유틸리티
python-dateutil==2.8.2
simplejson==3.19.2
urllib3==2.1.0

# 로깅
python-json-logger==2.0.7